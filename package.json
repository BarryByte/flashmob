
{
  "name": "flashmob",
  "version": "1.0.0",
  "private": true,
  "scripts": {

    "build": "npm install && npm run build --prefix flashmob-app",

    "dev": "concurrently \"npm run dev --prefix flashmob-server\" \"npm run dev --prefix flashmob-app\"",
    "build-frontend": "npx vite build --prefix flashmob-app"

  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
